### Variables
@baseUrl = {{baseUrl}}

### Health Check - This should return a 200 OK status
GET {{baseUrl}}/health

### Test with environment variable
# @name healthCheck
GET {{baseUrl}}/health

### Print response status
GET {{baseUrl}}/health
> {%
    if (response.status === 200) {
        client.log("✅ API is running correctly!");
    } else {
        client.log("❌ API is not responding properly. Status: " + response.status);
    }
%}